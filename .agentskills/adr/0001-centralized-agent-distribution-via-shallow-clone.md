# Centralized Agent Distribution via Shallow Clone

We decide to distribute specialized subagents and templates via a shallow-clone mechanisms from a centralized upstream repository. This ensures maximum consistency across workspaces while avoiding excessive disk usage.
